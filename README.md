2DMidpointDisplacement
======================

Creates a landscape randomly. Python implementation of the midpoint displacement algorithm.

Relies on the Myro Graphics library, or the Calico IDE (which includes that).
